# This file is deprecated.
# The new recommendation system in src/recommend.py uses on-the-fly computation with caching.
# You no longer need to run a separate training script for Streamlit Cloud.

def train():
    print("Training is now handled automatically with caching in the Streamlit app.")

if __name__ == "__main__":
    train()
